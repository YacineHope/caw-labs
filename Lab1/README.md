
Exercise 3 of Lab1:

5. Pseudo-classes and Pseudo-elements:

4th question : the purpose of :focus pseudo-class is to apply styles  once there is an interaction with an element like clicking on the input fields in this case.

5th Question's explanation: so when we click on the input fields (the event), they will have a blue border indicating they are focused(the action).

6. Combining Selectors & Specificity Challenge:

2nd Question's explanation: so the rule who take precendence is footer h2{}, because it is more specific ,and because we specified the h2 who are inside the footer (in another meaning the style "which is the font-size property in this case" of h2 element specified by the parent element which is the footer) to have their own propeties to apply on itselves, and if we delete footer h2{} the h2{} rule will apply to all h2 elements so it will be generalised not specified  by an element (classes or ids) that can determine their own styles.
